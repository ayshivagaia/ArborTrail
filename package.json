{
  "name": "arbor-gaia",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "if [ -z \"$API_KEY\" ]; then echo \"Warning: API_KEY is empty during build.\"; else sed -i \"s/API_KEY_PLACEHOLDER/$API_KEY/g\" index.html; fi"
  }
}